{
  "name": "ambrosia-pos-electron",
  "version": "1.0.0",
  "description": "Ambrosia-POS Desktop Application",
  "main": "main.js",
  "scripts": {
    "dev": "electron .",
    "dev:inspect": "electron --inspect=5858 .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "build:prepare": "node scripts/prepare-resources-wrapper.js",
    "build": "npm run build:prepare && electron-builder",
    "pack": "npm run build:prepare && electron-builder --dir",
    "dist": "npm run build:prepare && electron-builder",
    "dist:mac:arm64": "cross-env TARGET_PLATFORM=macos-arm64 npm run build:prepare && electron-builder --mac",
    "dist:mac:x64": "cross-env TARGET_PLATFORM=macos-x64 npm run build:prepare && electron-builder --mac",
    "dist:win": "cross-env TARGET_PLATFORM=win-x64 npm run build:prepare && electron-builder --win",
    "dist:linux": "cross-env TARGET_PLATFORM=linux-x64 npm run build:prepare && electron-builder --linux",
    "postinstall": "electron-builder install-app-deps"
  },
  "author": "olympus-btc",
  "homepage": "https://github.com/olympus-btc/ambrosia",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "cross-env": "^10.1.0",
    "electron": "^36.1.0",
    "electron-builder": "^25.1.8",
    "eslint": "^9.39.2",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-n": "^17.23.1"
  },
  "dependencies": {
    "bip39": "^3.1.0",
    "cross-spawn": "^7.0.6",
    "find-free-port": "^2.0.0",
    "tree-kill": "^1.2.2",
    "wait-on": "^8.0.1"
  },
  "build": {
    "appId": "com.olympus-btc.ambrosia-pos",
    "productName": "AmbrosiaPos",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "asarUnpack": [
      "services/**/*",
      "utils/**/*"
    ],
    "files": [
      "main.js",
      "preload.js",
      "package.json",
      "services/**/*",
      "utils/**/*"
    ],
    "extraResources": [
      {
        "from": "resources",
        "to": ".",
        "filter": [
          "**/*"
        ]
      }
    ],
    "mac": {
      "category": "public.app-category.business",
      "target": [
        "dmg",
        "zip"
      ],
      "icon": "build/icon.icns",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "build/icon.ico"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Office",
      "icon": "build/icon.png",
      "maintainer": "Ivan Robles <contact@ivanrobles.pro>"
    },
    "deb": {
      "afterInstall": "build/postinst"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
